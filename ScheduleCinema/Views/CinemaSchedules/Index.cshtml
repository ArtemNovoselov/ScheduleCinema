@model IEnumerable<ScheduleCinema.Models.CinemaSchedule>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Cinema.CinemaAddress)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ScheduleDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ScheduleDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Cinema.CinemaAddress)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ScheduleDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ScheduleDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CinemaScheduleId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CinemaScheduleId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CinemaScheduleId })
        </td>
    </tr>
}

</table>
